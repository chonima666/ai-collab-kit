# AI 協作工程守則

正式版 v1.0 · 2026-09-23 · @s

本守則規範 AI 與人工協作者的授權、施工、驗證及交接方式，適用於所有工程專案。專案的架構、技術、必跑測試、提交門檻與發布流程，由專案文件記載。專案文件可訂定比本守則更嚴格的要求；兩者衝突時，依第 1 節列明差異，由委託人決定。開始工作前先閱讀本守則及專案文件；發現可重複適用的新教訓，修訂守則並在第 10 節記錄變更原因。

---

## 1. 事實來源與起點

程式碼說明實際寫了什麼，測試說明在指定條件下跑出什麼結果，部署狀態說明現場跑的是什麼；文件和交辦文字說明預期與背景。依問題查最直接的一手證據，不把任何一種來源當成永遠優先的真相。證據衝突時，列明差異、查證時間、環境及分支，必要時請委託人決定；不可默默選邊。

跨對話或多人工作開始前，檢查工作樹、現有分支、標籤、遠端分支及其他進行中的工作。遠端不可用時記錄此限制，不得聲稱沒有平行工作。既有專案不得只憑交接文字推斷進度。

**工作歸屬與重疊處理**：開工時，在地圖節點或追蹤紀錄等共用可見的位置，記錄任務 ID、負責人、分支及預計碰觸的節點。發現他人正在修改相同檔案、相同節點或同一條控制流程時，先與對方或委託人協調修改界線與整合順序，並記錄協調結果，才繼續重疊的部分；不重疊的部分照常施工。無法聯繫對方或協調未有結果時，重疊部分列為第 3 節的阻塞停點。

**新對話狀態校準**：

1. 讀最新版 `PROJECT_MAP.md` 中與任務相關的節點：先讀樹狀索引，再沿受影響的依賴展開，發現跨模組影響時才擴大讀取範圍。另讀最近一份 HANDOFF 的第 10 段（下一步）與第 12 段（現狀安全性）；第 5、6、9、11 段若涉及本次工作，一併讀取。上次只留 Mini-Handoff 時，讀取地圖節點所連結的 Mini-Handoff 檔案（見第 5 節）。
2. 核對目前分支、工作樹、必要的測試結果與遠端狀態，以及相關節點上其他人的工作歸屬。
3. 標出自交接後的變動及未知事項。
4. 接手他人工作時，核對完畢後才把節點上的 `待接手 TASK-n` 改回 `▶ 本次 TASK-n`，並更新負責人（見第 4.2 節）。

前次對話的記憶只作搜尋線索，不能代替現場核對。地圖或 HANDOFF 缺失時，從現有證據建立本次工作的最小起點，毋須先補完歷史。

---

## 2. 授權與範圍

開始多步驟工作時，用一兩句記錄本次目標、授權範圍、排除事項，以及可檢查的完成條件。已授權的施工持續完成，不逐步索取許可。範圍外發現先記錄於追蹤項目；只有同時符合第 3 節「低風險直接修復」的全部條件，才可直接補修。

**需另行授權的動作**：修 bug 的授權不等於合併、正式發布或改權限的授權。以下動作須先備妥可審查的結果，再就該動作取得當下的明確授權：

- 建立正式發布標籤
- 合併共用或受保護分支
- production 部署
- 刪除資料或分支
- 強制覆蓋（如 force push、覆寫遠端歷史）
- 權限邊界變更

專案依 `REVIEW_PROTOCOL.md` 第 10 節啟用自動交付時，委託人以 ruleset 與 Policy Gate 事先授權符合條件的合併：這類合併由 workflow 執行，不是 AI 的動作，AI 本身仍不得合併。Policy Gate 未放行的合併照本節辦理。

授權依第 3 節留下決策紀錄。本次對話已明確授權過的動作，若對象（分支、版本、環境、資料範圍）與附帶條件都相同，不必重複詢問；任一項改變即視為新動作，須重新取得授權。

---

## 3. 風險分級與停點

每項施工先記錄分類及理由。分類不確定時，一律按較高等級處理。

- **低風險直接修復**：須同時符合以下五項條件：(1) 純新增；(2) 不改既有控制流程；(3) 不涉及安全或審批邊界；(4) 無不可逆的資料變更；(5) 不擴大權限、資料可見範圍或外部暴露面。按第 6 節驗證後回報。
- **一般施工**：在本次授權內且未觸及 Gate。可修改既有功能，並以測試覆蓋受影響的流程；按第 6 節驗證後回報。
- **Gate**：涉及安全、授權、審批、計費或專案列為關鍵控制的邏輯；涉及 production 資料、正式發布標籤、不可逆操作，或沒有可靠復原途徑的操作（見第 8 節）；建立或改變安全邊界；或核心前提與查證結果真正衝突。列出發現、影響、可選方案、建議及決策人，等明確答覆才執行該部分。

**Gate 授權請求卡片**：遭遇 Gate 或第 2 節需另行授權的動作時，以下列格式向決策人提出請求，答覆前不得執行該部分：
```text
【GATE 授權請求】
- 任務／節點：
- 遭遇原因：
- 請求的動作與對象：（動作、環境、分支或資料範圍）
- 依據版本：（commit 或方案）
- 影響範圍：
- 預期風險與回退：（見第 8 節回退計畫）
- 可選方案與建議：
- 決策人：
- 決策：[ ] 批准 / [ ] 拒絕 / [ ] 有條件批准（附帶條件：＿＿）
```
卡片是提出請求的格式，不是授權本身。勾選只對卡片所列的動作、對象與版本有效；任一項改變須重新提出。決策人答覆後，依下方「決策紀錄」寫入追蹤紀錄。

**決策紀錄**：Gate 的答覆與第 2 節的授權，一律寫入追蹤紀錄，內容包括決策人、時間、批准的動作與對象、依據的 commit 或方案，以及附帶條件。口頭或對話中的答覆同樣要寫入。依據的 commit、方案或前提改變時，重新判斷是否仍在批准範圍內；超出範圍即視為新的 Gate 或新動作。

真正的停點僅限以下五種：Gate；必要資訊或外部依賴阻塞；環境或權限限制；第 2 節需另行授權的動作；與他人工作重疊且尚未協調完成（第 1 節）。局部停點不妨礙其他互不依賴且已授權的工作。發送進度訊息不是停點。

---

## 4. 專案地圖與施工定位

### 4.1 既有專案探索與開工建圖
對缺少文件、測試或清楚模組界線的既有專案，可先進入**探索性研究階段（Spike Phase）**：以唯讀方式檢查入口、相關呼叫鏈、依賴、現有測試和部署線索，先產出覆蓋本次任務的「最小可用範圍地圖」。尚未盤點的區域標灰色「待盤點」；已知模組存在但其行為與邊界未知時，同樣標灰色並加註 `未知遺留模組`，不得推斷它安全或已完成。探索中發現的高風險控制或核心前提衝突，依第 3 節處理。唯讀探勘不等於授權修改，也不等於授權執行具有副作用的指令。

探索的結束條件是：能定位本次修改點、相鄰依賴與可能受影響的控制邊界，並列出可執行的驗證及未知項；不是整個程式庫都已看完。急迫修復可在留下這份局部地圖後繼續，不因盤點其餘模組而延誤。

`PROJECT_MAP.md` 以「子系統 → 模組／服務 → 主要能力」的樹狀清單作為索引，只畫到能指派工作與驗證責任的粒度，避免逐檔展開。每個節點須有：穩定 ID、名稱、程式碼位置、負責的能力、目前狀態（附任務 ID）、工作歸屬、驗證證據。

跨模組依賴、安全邊界和外部服務另用 Mermaid 依賴圖表示，不硬塞進樹狀階層。依賴圖只畫已查證的結構；尚未查證的連線加註 `待查`，計畫中的模組加註 `規劃`，不得畫成已存在。依賴圖沿用樹狀清單的節點 ID，只表示結構與邊界，不標工作狀態；工作狀態只在樹狀清單一處維護。

大型專案先畫與本次任務相關的範圍，其他區塊暫標「待盤點」；不因製圖阻擋修復。地圖是導航索引，程式碼與驗證紀錄仍是事實依據。

### 4.2 標記規則
**狀態描述的對象**：灰色「待盤點」描述節點本身是否已盤點；其餘顏色一律描述**某個任務**在該節點上的進度，不描述模組本身，書寫時必須附任務 ID。綠色另須附驗證基準，例如 `API-2 [綠／TASK-12 已完成，基準 commit a1b2c3d]`。驗證基準在已提交時為 commit；尚未提交時為工作樹狀態（基底 commit 及未提交變更摘要）與檢查時間。提交後重新核對驗證結果，再把基準更新為該 commit。專案規定提交後才算完成時，提交前維持黃色。「已完成」只表示該任務在該基準上達到完成條件，不代表模組往後不會再改，也不代表後續 commit 仍符合當時的驗證。同一節點開始新任務時，狀態改為新任務的進度，舊任務的完成證據移入該節點的證據欄保留，不得抹除。

**工作狀態**：顏色只表示工作狀態，且永遠同時顯示文字，避免黑白列印或色覺差異造成誤讀。

| 顏色 | 文字狀態 | 判定依據 |
| --- | --- | --- |
| 灰 | 待盤點 | 尚未核對節點的實際實作或任務範圍 |
| 白 | 未開始 | 已盤點，任務已納入計畫但尚未施工 |
| 藍 | 施工中 | 任務已開工，實作尚未完成 |
| 黃 | 待驗證 | 實作完成，必要驗證尚未完成或進行中 |
| 綠 | 已完成 | 任務在記錄的驗證基準上達到預先定義的完成條件，並附證據 |
| 紅 | 阻塞／Gate | 已記錄無法繼續的原因或等待中的決策 |

**補充標籤**：以下資訊不用顏色表達，一律以文字標籤註明。
- `混合`：父節點底下的子節點狀態不一。父節點不塗色，不得因部分子項完成就塗綠整個父節點
- `▶ 本次 TASK-n`：有人正在處理此節點，每個都須對應任務 ID，可同時標多個節點。以 Mini-Handoff 暫離或換對話時保留；任務完成時移除
- `待接手 TASK-n`：已正式交接、接手者尚未核對現場。接手者依第 1 節校準後，改回 `▶ 本次 TASK-n` 並更新負責人
- `負責: …`、`分支: …`：工作歸屬，見第 1 節
- `未知遺留模組`：已知存在，但行為與邊界未知；搭配灰色
- `規劃`：計畫中、尚未存在的模組；搭配白色
- `待查`：依賴圖中尚未查證的連線或結構
- `待核對`：地圖與程式碼衝突，差異已記錄、等待依證據修訂；維持原顏色
- `未結案`：緊急修復逾期未補齊紀錄（見第 8 節）；維持原顏色
- `風險: …`、`環境: …`：風險等級、部署環境等其他屬性，例如 `風險: Gate`、`環境: staging`

### 4.3 更新節點與交接
每次開始施工，先依第 1 節記錄工作歸屬，並指出任務 ID、節點 ID、預計完成條件及所需驗證。進度更新時只報告**本次變更節點、原狀態 → 新狀態、證據或阻塞原因**，不在對話中重印整張圖；權威地圖仍須同步更新，差分訊息不能取代文件。

任務完成時，更新節點狀態、證據（測試指令及結果、commit／PR、部署版本、驗證基準）、待辦與檢查時間，並移除 `▶ 本次`。未完成而以 Mini-Handoff 暫離時，依第 5 節規定儲存並保留 `▶ 本次`；未完成而以完整 HANDOFF 交接時，改為 `待接手`，維持原狀態色並寫明下一步。若地圖與程式碼衝突，先加註 `待核對` 並記錄差異，再依證據修訂；不得靠改顏色宣稱完成。

地圖宜與程式碼放在同一 repository 中版本管理；若無 repository，放在共用的專案文件位置，並記錄更新者與時間。地圖更新與對應實作在同一次交付中完成。正式 HANDOFF 第 11 段列出地圖變更，第 12 段說明是否可安全接手。

### 4.4 最小範例
```text
專案地圖（檢查：2026-09-23；基準：本機 main）
APP  應用程式 [混合]
├─ AUTH  身分與權限 [白／TASK-15 未開始]  src/auth/  風險: Gate
├─ API   對外介面   [混合]  src/api/
│  ├─ API-1 查詢訂單 [綠／TASK-8 已完成，基準 a1b2c3d]  src/api/orders/
│  └─ API-2 新增訂單 [藍／TASK-12 施工中]  src/api/orders/
│        ▶ 本次 TASK-12  負責: @s  分支: feat/task-12
├─ DB    資料層     [黃／TASK-10 待驗證]  src/db/  環境: staging
│        待接手 TASK-10
└─ LEGACY 舊報表    [灰／待盤點]  legacy/report/  未知遺留模組
```
此圖僅示範狀態寫法，並非任何實際專案的進度；為求精簡省略了「負責的能力」與證據欄位，實際地圖須依第 4.1 節填齊。

---

## 5. 執行節奏與回報

範圍內且未觸及 Gate 的工作持續完成。依情境使用下列格式：

- **短進度回報**：目前節點／已做／驗證狀態／下一步。發送後繼續施工。
- **Mini-Handoff**：內容包含 `[目前節點]`、`[已做事項]`、`[驗證狀態]`、`[未提交改動摘要]`、`[正在執行的程序]` 與 `[下一步具體指令]`；節點保留 `▶ 本次`。
- **完整 HANDOFF**：使用第 7 節格式；未完成的節點改標 `待接手`。

**Mini-Handoff 存放規範**：Mini-Handoff 按任務各自存成獨立檔案，例如 `.ai/handoffs/TASK-12.md`；專案文件只指定存放目錄，地圖節點連結到對應檔案。同一任務的後續 Mini-Handoff 覆寫該任務的檔案，不寫入其他任務的檔案，避免多人或多個 AI session 同時施工時互相覆蓋。任務完成後，依專案規定保留或封存該檔案。新對話接續時，依節點連結讀取對應檔案以還原現場。

**微型任務**：不中斷、同次完成的單一小變更，不另建 Mini-Handoff，在 commit 訊息或簡短回報中記錄受影響節點與驗證結果即可；節點狀態因此改變時，仍依第 4.3 節更新地圖。「小」只影響回報格式，不降低第 3 節的風險分級，例如只改一行的權限修改仍可能是 Gate。

選用格式的判準是**是否換接手者**，以及**是否可能失去現場脈絡**，而不是是否跨日或換對話：
- 短暫中斷，或單純換對話、仍由同一負責人接續，且地圖、工作樹與該任務的 Mini-Handoff 檔案足以還原狀態時，用 Mini-Handoff。
- 換負責人或協作者、停在第 3 節所列的真正停點、重大里程碑，或預期長時間擱置而可能失去現場脈絡時，用完整 HANDOFF。

恢復時一律依第 1 節核對現場；現場與 Mini-Handoff 不符時，記錄差異並依現場證據重建起點。範圍外發現依第 2 節記錄於追蹤項目，主任務照常進行。

**反覆失敗與方向調整**：同一方案經修正後仍以相同或相近方式反覆失敗，或新證據推翻原本的假設時，暫停該方案，不再原地重試。先記錄已做的嘗試、錯誤證據、尚未排除的原因，以及下一個可行方向，再換方向繼續。

專案文件未另訂門檻時，預設以下兩個**檢查觸發點**。觸發後是重新檢查，不是機械式停工；暫停的只是該方案，其他已授權的工作照常進行。

- 以完全相同的指令與條件失敗一次後，不得無理由重跑。確有理由（例如疑似偶發的環境問題）時，先寫明理由再重跑。
- 同一方案連續失敗 3 次時，必須重新檢查假設並記錄差異，再決定繼續、換方向或停下。微調參數、改寫同一指令都不會重置這個計數；只有提出新的原因假設，並說明下一次嘗試要驗證什麼，才算換方向，計數從新方向重新開始。

相似的例外錯誤可能出自不同原因，不自動判定為同一問題。

暫停或換方向時：

- 可回到已知安全基準（最近一次驗證通過的 commit 或工作樹狀態）重新開始。未提交的改動先另存（例如 stash 或備份分支）再離開，不直接丟棄。
- 刪除分支、捨棄改動仍依第 2 節取得授權，不因「實驗失敗」而自動執行。
- 提不出可區分的新假設、沒有可行方向，或換方向會超出本次授權範圍時，列為第 3 節的阻塞停點，向委託人回報；不另設強制停工的次數。

---

## 6. 工程驗證與提交

1. **一手證據**：測試執行器的原始 stdout／stderr、退出碼、指令、時間、環境與 commit（尚未提交時為基底 commit 及未提交變更摘要），是該次執行的一手證據；CI 結果另記錄工作流程名稱與對應 commit。
2. **敏感資訊**：保存證據前，檢查並遮蔽憑證、token、密碼、個資等敏感值，遮蔽處註明「已遮蔽」。HANDOFF 與對話中只記錄指令、退出碼、測試摘要，以及原始紀錄的存放位置與存取方式，不貼完整輸出。
3. **分層記錄**：實作、單元測試、整合測試、實際環境（staging／production）分開記錄條件與結果。測試通過只證明所覆蓋的案例在該環境通過，不能推論 staging 或 production 已驗證；某一層在實際驗證前一律標「未驗證」。
4. **變更前基準**：修改缺乏既有測試的行為前，可行時先記錄現有行為與重現步驟（輸入、操作、實際輸出），再針對受影響的行為加測試。無法安全執行時，標「未驗證」並寫明原因。現有行為只是觀察紀錄，不自動等於正確需求；現有行為疑似錯誤時，是否保留由委託人決定。
5. **提交前檢查**：每次提交前執行專案定義的必跑檢查，以及可取得的真實後端驗證。跑不到的項目記為「未驗證」並記錄限制與已驗證的部分，依專案提交門檻判斷能否提交，不冒稱通過；也不把等待 staging 當成所有局部工作的停點。
6. **格式與靜態檢查**：提交前執行專案列出的 formatter、linter、型別檢查或其他靜態檢查。區分既有警告與本次新增的警告；本次新增的依提交門檻處理，既有警告不強制本次清除。專案未設定靜態檢查基準檔（baseline）時，修改前以同一工具版本、設定與檢查範圍執行並保存結果，作為既有警告的基準快照；修改後比對找出新增警告。快照須記錄基底 commit、工具與設定版本、檢查範圍、執行時間、報告存放位置及保存期限；存放位置由專案指定，不強制提交進程式碼庫。修改前跑不到時，標示「無基準，無法判定新舊」，不推測。基準報告同樣依本節第 2 條遮蔽敏感資訊。不得為了讓工具通過而大幅修改範圍外的程式碼；範圍外問題依第 2 節記錄。專案沒有這些工具時，記錄為缺口，不假裝已檢查。
7. **可辨識的錯誤**：可能由多個獨立條件導致失敗的操作，錯誤訊息應能辨明是哪個原因。
8. **清理孤兒資源**：移除某功能的唯一使用者時，檢查其依賴資源是否仍被使用；孤兒資源同次清理，或明列保留理由。清理若觸及 Gate，先停下該部分。
9. **人工前置步驟**：必須提供可實際操作的方式（指令、腳本或介面）；只有文件說明不算實作完成。
10. **Commit 粒度**：每個 commit 保持單一、可獨立審查與還原的目的；測試與文件若服務同一變更，可與實作放在同一 commit。

平行施工前的遠端檢查與重疊處理，依第 1 節辦理；無法確認時標記資訊缺口。

---

## 7. 正式 HANDOFF（十二段）

使用時機見第 5 節。每段都要填寫，無內容寫「無／不適用」。完整 HANDOFF 存為可查找的文件，對話中只需提供位置與必要摘要；接手者依第 1 節校準。

1. **任務目的**：一句話說明預期成果。
2. **範圍確認**：授權來源、時間、包括及排除事項；負責人、分支及預計碰觸的節點。
3. **完成項目**：完成的具體工作、對應任務 ID 及驗證基準（見第 4.2 節）。
4. **低風險直接修復**：項目，及符合第 3 節五項條件的理由。
5. **Gate 與待授權動作**：阻塞中的 Gate 及第 2 節尚待授權的動作，含原因、影響與決策人；本次已取得的決策紀錄位置。
6. **驗證矩陣**：逐功能列出 實作／單元／整合／staging／production 各層狀態（已驗證、失敗、未驗證、不適用），附證據摘要與原始紀錄位置（依第 6 節遮蔽敏感資訊）。
7. **發現的問題／缺陷**：範圍外問題與影響。
8. **已知風險／代價**：殘留風險及技術債。
9. **需要人工決定的項目**：不阻塞目前施工的決策；選項、建議與等待的答覆，並明列刻意延後的事項。
10. **下一步計畫**：可直接進行的工作，及待放行的工作。
11. **受影響的檔案／產物**：檔案、地圖節點變更、分支、commit／PR／版本資訊。
12. **現狀安全性確認**：工作樹與部署狀態、已執行變更的回退方法，以及是否可安全中斷及接手。

---

## 8. 發布、回退與收尾

**回退計畫**：部署、migration、資料修改及其他改變共享狀態的操作，執行前記錄：

- **服務功能的復原方式**：回退方法或前進修復方案。
- **資料狀態的復原方式**：回退、備份還原，或註明不涉及資料變更。復原若依賴備份，須附該備份的還原演練（Restore Drill）證據與時間。
- **尚未演練或驗證之處**（例如備份是否實際可還原）。
- **觸發回退的條件**。
- **負責執行回退的人**。

依影響分級處理：
- **列為 Gate**：不可逆的資料變更、沒有可靠復原途徑的操作，或復原計畫依賴備份、但該備份的還原演練證據不足（沒有演練紀錄，或超出專案訂定的有效期限）的操作，依第 3 節由決策人在知情下決定是否執行。不依賴備份復原的操作，不因此條升級，依原有分類處理。
- **依發布門檻**：一般可逆的部署（例如可重新部署前一版本，且不涉及無法還原的資料變更）記錄上述內容後，依專案發布門檻決定是否執行；未演練之處寫入 HANDOFF 第 8 段。

判斷復原途徑時，須分別檢查服務功能與資料狀態能否恢復。服務功能可由回退或前進修復恢復；資料狀態須有可用的回退方法或經確認可還原的備份。前進修復若無法恢復受影響的資料，不得據此認定操作可逆。例如程式可以再修一次，但已被覆寫且沒有備份的資料仍無法還原。

可逆不代表免除授權：第 2 節所列動作（如 production 部署）仍須另行授權。執行後依觸發條件判斷是否回退，並記錄結果。

**緊急修復（Hotfix）**：是否屬於緊急情況，由委託人或專案事故流程指定的人認定，AI 不得自行宣告。緊急情況下可以簡化記錄的形式與時點，但不能省略授權，也不能事後才留下首次授權證據：

- **事前**：留下事故、目標、改動範圍、決策人及回退點，可用精簡版的 Gate 授權請求卡片。
- **授權**：涉及 production 部署或 Gate 的明確授權不能略過，且須在執行前取得。緊急當下至少留下可追溯的最低紀錄：決策人、時間、批准的動作、環境與版本。完整的授權卡片、決策紀錄整理與 HANDOFF 可事後補齊；事後能補的是正式整理，不是首次授權證據。
- **事後**：補齊驗證證據、地圖與 HANDOFF。補記期限由專案事故流程訂定；未訂定時，預設在事故穩定後的下一個工作天結束前補齊。
- **逾期**：在追蹤紀錄與相關地圖節點加註 `未結案`，並通知指定的決策人。後續發布或再次使用緊急簡化流程前，須先處理缺口，除非決策人記錄例外。一般施工不因逾期而自動暫停。
- **例外**：決策人核准例外時，須記錄允許的具體發布或操作、尚缺的紀錄、接受例外的原因、補齊負責人及明確期限。例外只涵蓋所記錄的發布或操作；期限到仍未補齊時例外失效，不自動延長，需要延長須由決策人重新核准並記錄。

**整合與衝突處置**：

- 整合前先取得遠端最新狀態。
- 在隔離的工作樹或分支上解決衝突，不直接在共用分支上處理。
- 解完後檢查語意變化，並重跑受影響的驗證；即使沒有文字衝突，兩邊的修改合在一起仍可能改變行為。整合後的 commit 與原驗證基準不同時，依第 4.2 節重新核對。
- 衝突涉及 Gate 邏輯，或使既有授權的依據（決策紀錄中的 commit 或方案）失效時，停下該部分，依第 3 節重新決策。
- 採用 merge 或 rebase 依專案規定；已共享的分支不得為了整合而改寫歷史，除非依第 2 節取得強制覆蓋的授權。

推送、合併或建立標籤前，再次核對遠端狀態；遇到拒絕時先查明來源與歷史，未經第 2 節授權不得強制覆蓋。正式發布標籤一旦建立，不得移動或重用；優先採用附註標籤（annotated tag），以保留建立者與說明。這是協作政策，不能假設工具會替團隊強制執行。

收尾時依第 4.3 節核對地圖、實際變更與證據一致，列明未完成及待決事項，確認目前狀態能否安全交接。正式發布、合併及部署仍依第 2、3 節的授權與 Gate 辦理。

---

## 9. 專案落地清單

專案建立或接手時依序完成：

1. 指定委託人，以及各類 Gate 的決策人。
2. 指定 `PROJECT_MAP.md` 的存放位置與維護者，以及追蹤紀錄（工作歸屬、決策紀錄、範圍外發現）的存放位置。
3. 指定 Mini-Handoff 的存放目錄（例如 `.ai/handoffs/`），每個任務一個檔案。
4. 由專案文件定義：必跑測試命令、formatter／linter／型別或靜態檢查命令（沒有的記為缺口）、緊急修復的認定者與補記期限（未訂時依第 8 節預設）、環境取得方式、提交門檻、「完成」是否包含 staging 或 production 驗證、備份還原演練的有效期限、測試原始紀錄與靜態檢查基準快照的存放位置及保存期限，以及敏感資訊遮蔽方式。
5. 既有未知系統先依第 4.1 節做局部探索，為相關節點填入：節點 ID、程式碼位置、負責的能力、工作狀態、工作歸屬、依賴、安全邊界、驗證證據。
6. 第一個任務開始前，盤點相關節點與遠端施工狀態；此後每次開工用節點 ID 定位並記錄工作歸屬，收工時同步更新節點與 HANDOFF。

---

## 10. 修訂紀錄

| 版本 | 日期 | 修訂者 | 變更與原因 |
| --- | --- | --- | --- |
| r2 | 2026-09-23 | @s | 統一低風險條件為五項（原 HANDOFF 誤寫四項）；補充標籤獨立成表，收納原散落各節的「待查／規劃／待核對／未知遺留模組／混合」；區分藍與黃的判定；驗證矩陣補 production 層；「同一動作」免重複授權限定為對象相同；新增分類不確定從高、守則與專案文件衝突處理、決策人指定；刪除第 5、6、8 節重複敘述。 |
| r3 | 2026-09-23 | @s | 依條文缺口檢查（非已發生事故）補入：工作歸屬與重疊處理（第 1 節，新增第五種停點）；決策紀錄（第 3 節）；節點狀態一律附任務 ID、綠色附驗證基準，新任務不抹除舊證據（第 4.2 節）；新增 `待接手` 標籤，修正「▶ 本次 收工移除」與跨日未完成的矛盾；敏感資訊遮蔽與變更前基準（第 6 節）；回退計畫，回退不可行或未確認列為 Gate（第 8 節）；HANDOFF 與落地清單同步對應欄位。 |
| r4 | 2026-09-23 | @s | 修正三個會讓日常施工卡住的判定條件：回退改按影響分級，只有不可逆資料變更或無可靠復原途徑才列 Gate，一般可逆部署依發布門檻；綠色的「基準 commit」放寬為「驗證基準」，未提交時記錄工作樹狀態與檢查時間；HANDOFF 格式改以是否換接手者、是否可能失去脈絡為判準，單純換對話可用 Mini-Handoff，但須存入可查找位置。 |
| r5 | 2026-09-23 | @s | 釐清第 8 節「可靠復原途徑」：原定義把 "可執行的前進修復" 視為足以認定可逆，但前進修復無法還原已覆寫的資料。改為分別檢查服務功能與資料狀態能否恢復；回退計畫的記錄項目同步拆成服務與資料兩項。 |
| r6 | 2026-09-23 | @s | 強化工程落地的實務執行細節：(1) Mini-Handoff 規範統一存放於根目錄 `.ai/mini-handoff.md` 並落實至落地清單；(2) 引入標準化 "Gate 授權請求卡片" 格式以供遭遇風險時快速審批；(3) 嚴格規定回退計畫的資料狀態必須包含最近一次實際備份還原演練（Restore Drill）時間，未演練者自動升級為 Gate。 |
| r7 | 2026-09-23 | @s | 修正 r6 兩處會造成衝突的規則：(1) Mini-Handoff 由單一共用檔改為每任務獨立檔案，避免平行施工互相覆蓋；(2) 還原演練要求限定於復原計畫依賴備份的操作，「近期」改由專案訂定有效期限，不依賴備份的操作不受影響。Gate 卡片補上任務／節點、動作與對象、依據版本及決策人，並明定卡片只是請求格式，授權仍依決策紀錄。Mini-Handoff 恢復 r5 的「正在執行的程序」欄位。 |
| r8 | 2026-09-23 | @s | 補三項實務細節：(1) 微型任務免建 Mini-Handoff，改在 commit 或簡短回報記錄受影響節點與驗證結果，並明定規模小不降低風險分級；(2) 地圖按需讀取，先讀索引、沿依賴展開，跨模組影響才擴大；(3) 第 8 節新增整合與衝突處置：先取遠端最新狀態、隔離解衝突、檢查語意變化並重跑驗證，涉及 Gate 或使授權依據失效時重新決策；不規定一律 rebase，已共享分支不得改寫歷史。 |
| r9 | 2026-09-23 | @s | 補三項實務規則：(1) 第 5 節新增反覆失敗與方向調整：暫停失敗方案並記錄嘗試與證據，可回到已知安全基準，未提交改動先另存；刪分支、捨棄改動仍需授權，無可行方向時列阻塞停點；(2) 第 6 節新增格式與靜態檢查，區分既有與新增警告，不為讓工具通過而大改範圍外程式碼，無工具記為缺口；(3) 第 8 節新增緊急修復：由委託人或事故流程認定，簡化記錄形式與時點但不略過 production 部署與 Gate 授權，補記期限由專案訂定。 |
| r10 | 2026-09-23 | @s | 為三項規則補上專案未另訂時的預設值，並避免機械式停工：(1) 反覆失敗設兩個檢查觸發點：相同指令與條件失敗一次後不得無理由重跑，同一方案連續失敗 3 次須重新檢查假設；相似例外不自動視為同一問題，只暫停該方案；(2) 既有警告在無 baseline 時以修改前的同條件執行結果為基準快照，跑不到則標「無基準」；(3) 緊急修復預設於事故穩定後下一個工作天結束前補記，逾期標 `未結案` 並通知決策人，後續發布或再用簡化流程前須處理，不自動暫停一般施工；並釐清緊急當下須留最低授權紀錄，事後只補正式整理。 |
| r11 | 2026-09-23 | @s | 補三處邊界，未新增章節或擴大 Gate：(1) 第 8 節緊急修復例外須記錄允許的具體發布或操作、尚缺紀錄、原因、補齊負責人及期限，期限到即失效、不自動延長；(2) 第 5 節明定微調參數或改寫同一指令不重置失敗計數，須提出新的原因假設並說明要驗證什麼才算換方向，提不出新假設即列阻塞，不另設強制停工次數；(3) 第 6 節基準快照須記錄基底 commit、工具與設定版本、檢查範圍、執行時間、報告位置及保存期限，存放位置由專案指定。 |
| v1.0 | 2026-09-23 | @s | 由 r11 正式定稿；正文內容不變，移除草案標示並建立正式版本號。 |
| v1.1 | 2026-09-25 | Claude（依 owner 指示） | 第 2 節補充自動交付：單人專案以 ruleset 與 Policy Gate 事先授權符合條件的合併，由 workflow 執行；AI 本身仍不得合併，未放行的合併照舊需要當下授權。 |
